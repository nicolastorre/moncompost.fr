FROM node:23

WORKDIR /app

COPY package.json yarn.lock ./
COPY packages/backend/package.json ./packages/backend/

RUN yarn install --frozen-lockfile

COPY packages ./packages

CMD ["yarn", "workspace", "backend", "run", "dev"]
